# frozen_string_literal: true

# Appfile - Fastlane App Configuration
# Central configuration for app identifiers and team settings

# App Bundle Identifier
app_identifier("com.easymo.mobilityApp")

# Apple ID (email associated with Apple Developer account)
apple_id(ENV["APPLE_ID"] || "your-apple-id@example.com")

# App Store Connect Team ID (if you're in multiple teams)
# Find at: App Store Connect → Users and Access → Team ID
itc_team_id(ENV["ITC_TEAM_ID"] || "XXXXXXXXXX")

# Apple Developer Portal Team ID
# Find at: developer.apple.com → Membership → Team ID
team_id(ENV["APPLE_TEAM_ID"] || "XXXXXXXXXX")

# ══════════════════════════════════════════════════════════════
# App Store Connect API Key Configuration (Recommended for CI/CD)
# ══════════════════════════════════════════════════════════════
# 
# Using API keys is more secure than username/password and doesn't
# require 2FA during CI builds.
#
# 1. Create API key at: App Store Connect → Users and Access → Keys
# 2. Download the .p8 file (only available once!)
# 3. Set these environment variables:
#    - APP_STORE_CONNECT_API_KEY_ID
#    - APP_STORE_CONNECT_API_ISSUER_ID
#    - APP_STORE_CONNECT_API_KEY_CONTENT (base64 encoded .p8 content)
#
# The Fastfile will use these via app_store_connect_api_key() action
